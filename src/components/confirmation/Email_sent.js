import React from 'react';

const Email_sent = () => {
  return (
    <div style={{display:"flex", justifyContent:"center",alignItems:"center"}}>
   <h1 style={{fontSize: ".9rem",
    textAlign: "center",
    marginTop: "5%"}}>Your temporary password has been sent to your email check your email login and change the password </h1>
    </div>
  );
};

export default Email_sent;